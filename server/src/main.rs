// the server's job is to implement game rules, eventually instructing the client how to configure
// itself during operation, receiving player input from client, and transmitting updated gamestate
// to client.
fn main() {
    println!("Hello, world!");
}
